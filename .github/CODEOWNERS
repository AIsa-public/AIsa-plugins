# Review *routing* only — GitHub auto-requests these reviewers. The merge policy itself is
# enforced by .github/workflows/ownership.yml:
#   plugins/<name>/**  -> that plugin's `owners` in plugin.aisa.yaml (must match the lines below)
#   everything else    -> repository admins
# CI checks that every plugin's line here agrees with its declaration.

*                        @lhymmEU @aisa-barry @fatflowers @liuhaonan00 @JONY-AISA

/plugins/aisa-search/    @zhenlonghe
/plugins/aisa-gtm/       @lhymmEU
